{
  "name": "init-sudoku-post3",
  "private": true,
  "workspaces": [
    "packages/contracts",
    "packages/web",
    "packages/puzzle-service",
    "packages/scores-service"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "echo \"Define per-package tests\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
