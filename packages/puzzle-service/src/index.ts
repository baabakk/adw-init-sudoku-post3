// puzzle-service — Puzzle Service
// Scope: Stateless HTTP service that generates uniquely-solvable Sudoku puzzles at three difficulty levels (easy, medium, hard) and validates a submitted solution. Exposes GET /puzzle?difficulty= and POST /validate. Owns puzzle generation and solution checking only.
// Owns: packages/puzzle-service
// This team builds its slice here each phase.
export {};
