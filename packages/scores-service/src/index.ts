// scores-service — Scores Service
// Scope: HTTP service that records completed-game results (player name, difficulty, time-to-solve) and serves a per-difficulty top-10 leaderboard. Exposes POST /scores and GET /leaderboard?difficulty=. Owns persistence of scores only.
// Owns: packages/scores-service
// This team builds its slice here each phase.
export {};
