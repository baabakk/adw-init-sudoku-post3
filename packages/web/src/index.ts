// web-client — Web Client
// Scope: Browser SPA that renders a 9x9 Sudoku board, accepts player input, validates moves on the client, fetches puzzles from the Puzzle Service, and posts completed-game results to the Scores Service. Contains presentation and input handling only, no server-side business logic.
// Owns: packages/web
// This team builds its slice here each phase.
export {};
